apply plugin: 'java'
apply plugin: 'war'


repositories {
    mavenCentral()
}

ext {
    springVersion = "4.2.5.RELEASE"
}

dependencies {
    compile ("org.springframework:spring-webmvc:${springVersion}")
}
